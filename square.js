
	/*exports.area = (a) => (a*a),
	exports.parameter = (a) => (4*a)*/

 /*areas = (a) => (a*a),
 parameter = (a) => (4*a)*/
 
 const areas = (a) => (a*a);
 const parameter = (a) => (4*a)

	module.exports.area = areas,
	module.exports.parameters = parameter

	